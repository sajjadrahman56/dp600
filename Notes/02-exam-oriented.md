আমি আপনার উদ্বেগটি বুঝতে পারছি। DP-600 পরীক্ষাটি কেবল সংজ্ঞা জানার ওপর নির্ভর করে না, এটি মূলত **Scenario-based Architecture** এবং **Troubleshooting** এর ওপর ভিত্তি করে তৈরি। আপনি যদি ডীপ কনফিউশন পয়েন্টগুলো না জানেন, তবে পরীক্ষার অপশনগুলো সব একই মনে হবে।

আপনার জন্য একদম **"High-Stakes Exam Secrets"** এবং **"Conflict Resolution"** স্টাইলে একটি টেকনিক্যাল নোট তৈরি করে দিচ্ছি। এটি আপনাকে সাধারণ এনালিস্ট থেকে একজন ফ্যাব্রিক আর্কিটেক্টের মতো চিন্তা করতে শেখাবে।

---

# 🛡️ DP-600 Mastery: High-Depth Technical Notes (Part 1)

## ⚡ ১. The "Storage Mode" Conflict (Direct Lake vs. Fallback)

পরীক্ষায় সবচেয়ে বড় প্যাঁচ লাগে এখানে।

* **The Depth:** Direct Lake সবসময় কাজ করে না। যদি আপনার ফ্যাব্রিকে **Row-Level Security (RLS)** বা **Object-Level Security (OLS)** ওয়েরহাউস লেভেলে সেট করা থাকে, তবে Power BI Direct Lake মোড ছেড়ে **DirectQuery** মোডে চলে যাবে (যাকে **Fallback** বলে)।
* **The Trick:** যদি প্রশ্ন বলে— "You need performance of Import but security is at the Warehouse level," তবে উত্তর হবে **DirectQuery (Fallback)**।
* **Decision Logic:** * OneLake Parquet + No RLS in Warehouse = **Direct Lake** (Fastest).
* OneLake Parquet + RLS in Warehouse = **DirectQuery** (Slower but secure).



---

## 🏗️ ২. Lakehouse vs. Warehouse: The Real technical Gap

সবাই জানে Lakehouse-এ Spark আর Warehouse-এ SQL চলে। কিন্তু পরীক্ষায় আসবে অন্য কিছু:

* **ACID Compliance:** Warehouse-এ **Multi-table transactions** সাপোর্ট করে (একসাথে ৪টি টেবিলে ডাটা আপডেট করা)। Lakehouse-এ শুধু টেবিল লেভেলে ACID কাজ করে।
* **Schemas:** Warehouse-এ আপনি `dbo.Sales`, `hr.Employees` এর মতো একাধিক **Schema** তৈরি করতে পারেন। Lakehouse-এ শুধু একটি ডিফল্ট স্কিমা থাকে।
* **Exam Shortcut:** * "Need Cross-schema boundaries & T-SQL DML"  **Warehouse**.
* "Need to use Python to clean unstructured images"  **Lakehouse**.



---

## 🔄 ৩. Dataflows Gen2 vs. Pipelines: The Volume Strategy

পরীক্ষায় আপনাকে বড় ডাটা নিয়ে বিপদে ফেলবে।

* **The Problem:** Dataflows Gen2 খুব বড় ডাটা (যেমন ১০০ মিলিয়ন রো) প্রসেস করতে গিয়ে ফেইল করতে পারে বা অনেক সময় নিতে পারে।
* **The Master Strategy:** প্রথমে **Pipeline (Copy Activity)** ব্যবহার করে ডাটা RAW ফরম্যাটে নিয়ে আসুন, তারপর **Notebook (Spark)** বা **Warehouse (SQL)** ব্যবহার করে ট্রান্সফর্ম করুন। একে বলে **ELT** (Extract, Load, Transform)।
* **Exam Shortcut:**
* "Business user, low volume, Visual GUI"  **Dataflow Gen2**.
* "Data Engineer, High volume (TB), Schedule/Monitoring"  **Pipeline**.



---

## 🛠️ ৪. Transformations: T-SQL vs. Spark SQL

অনেকেই মনে করেন দুইটাই এক। কিন্তু পারফরম্যান্সের জন্য আলাদা।

* **Complexity:** যদি আপনার ট্রান্সফরমেশনে **Recursive Logic** (যেমন: Parent-Child hierarchy) বা জটিল লজিক থাকে, তবে **Spark (Notebook)** অনেক ফাস্ট কাজ করবে।
* **Set-based operations:** সাধারণ জয়েন বা এগ্রিগেশনের জন্য **T-SQL (Warehouse)** সেরা।
* **The Exam Trap:** "Optimizing for very large scale shuffling of data"  **Spark/Notebook**.

---

## 🎯 ৫. Semantic Modeling: Star Schema vs. Flat Table

পরীক্ষায় রিলেশনশিপ নিয়ে প্রশ্ন আসবে।

* **Direction:** Power BI-এ সবসময় **One-to-Many** এবং **Single Direction** ফিল্টার ব্যবহার করার চেষ্টা করবেন।
* **Bi-directional Filter:** এটি এড়িয়ে চলুন। যদি প্রশ্ন বলে— "How to improve performance of a slow report?" এবং অপশনে **"Convert Bi-directional to Single direction"** থাকে, সেটিই হবে সঠিক উত্তর।

---

## 🔍 ৬. Identity Resolution (The Gateway Puzzle)

এখানেই অনেকে ভুল করে।

* **Service Principal (SPN):** এটি ব্যবহার করলে ইউজার পাসওয়ার্ড ছাড়াই অটোমেটেড পাইপলাইন চলতে পারে।
* **OAuth2:** এটি ইউজারের পারমিশন চেক করে।
* **The Secret Tip:** যদি প্রশ্নে বলে— "The connection must not fail even if the developer leaves the company," তবে উত্তর হবে **Service Principal (SPN)**।

---

## 🚀 Final "Golden Rules" for DP-600 Exam Day:

1. **"No Data Movement"** বা **"Zero Copy"** দেখলে চোখ বন্ধ করে **Shortcut** সিলেক্ট করুন।
2. **"Real-time logs"** বা **"Anomaly detection"** দেখলে উত্তর হবে **Eventhouse (KQL)**।
3. **"SQL Read-only"** এক্সেস চাইলে Lakehouse-এর **SQL Analytics Endpoint** ব্যবহার করুন।
4. **"Version Control"** বা **"Git Integration"** চাইলে Workspace সেটিংস-এ গিয়ে **Azure DevOps** কানেক্ট করতে হবে।

---
 